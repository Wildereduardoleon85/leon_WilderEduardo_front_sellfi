import { makeStyles } from '@material-ui/styles';

export const headerStyles = makeStyles({
    grow: {
      flexGrow: 1
    },
    imageContainer: {
        width: '40px',
        display: 'flex',
        alignItems: 'center'
    },
    imageIcon: {
        width: '100%'
    }
  });